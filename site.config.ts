type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Gujrera",
  site_description:
    "Gujrera — property listings, projects, and updates for Gujarat real estate.",
  site_domain: "https://next.gujrera.com",
};
