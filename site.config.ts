type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Next.js and WordPress Starter",
  site_description:
    "A starter template for Next.js with WordPress as a headless CMS.",
  site_domain: "https://next-wp.com",
};
