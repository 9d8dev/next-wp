type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Marty's Sandbox",
  site_description: "Starter template for Headless WordPress with Next.js",
  site_domain: "https://next-wp.com",
};
